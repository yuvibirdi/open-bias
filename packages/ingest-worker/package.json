{
  "name": "@open-bias/ingest-worker",
  "module": "index.ts",
  "main": "./src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
      "dev": "bunx tsc -w --pretty",
      "build": "bun build src/worker.ts --outdir dist",
      "lint": "bunx eslint ."
  }
}